<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <link rel="stylesheet" href="http://localhost:3001/modify.css">
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <title>Title</title>
</head>
<body>
    <%-include('header.ejs')%>

    <div class="container">

    </div>
    <div class="title-container">
        
    </div>

    <div class="main-container">
        <form method="POST" action="/update/?title=<%= rows[0].title %>&value=<%= rows[0].num %>&name=<%= rows[0].name %>">
            <table class="table">
                <tr class="top">
                    <th colspan="2">글 수정하기</th>
                </tr>
                <tr class="empty">

                </tr>
                <tr>
                    <th class="title">글 제목</th>
                    <td><input type="text" name="title" class="input-title" value="<%= rows[0].title %>" disabled></td>
                </tr>
                <tr>
                    <th class="name">작성자</th>
                    <td><input type="text" name="name" class="input-name"  value="<%= rows[0].name %>" disabled></td>
                </tr>
                <tr>
                    <th class="date">날짜</th>
                    <td ><input type="text" class="input-date" value="<%= rows[0].date %>" disabled></td>
                </tr>
                <tr>
                    <th class="content">내용</th>
                    <td><textarea class="input-content" rows="20" name = "content" value=""><%= rows[0].content %></textarea></td>
                </tr>
                <tr>
                    <td class="empty-td"></td>
                    <td class="button">
                        <input type="submit" value="완료" class="submit">
                        <input type="button" onclick="history.back()" value="취소" id="close">
                        
                    </td>
                    
                </tr>
                <tr class="empty">

                </tr>
                
            </table>

        </form>
    </div>

    <div class="container">

    </div>

    <%-include('footer.ejs')%>
    
</body>
</html>